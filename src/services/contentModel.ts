
import { ContentModel } from "../collections/contentsCollection.js";


export const addContent = async (data: object) => {

    // const { something: string } = data;
    await ContentModel.create({

    })
} 